require 'rails_helper'
require 'carrierwave/test/matchers'

describe ImageUploader do
  # before(:each) do
  #   @department = FactoryGirl.create(:department)
  #   @employee = FactoryGirl.create(:employee, department_id: @department.id)
  # end
  #
  # let(:uploader) { ImageUploader.new(@employee, :image) }
  #
  # it "should return the store directory" do
  #   expect(uploader.store_dir).to eq('uploads/employee/image/' + @employee.id.to_s)
  # end
end