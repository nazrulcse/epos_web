<% if @queue_code.errors.any? %>
popupMessage('<%= escape_javascript( errors_to_message_string(@queue_code.errors)).html_safe %>', 'alert-danger');
<% else %>
$('#global-modal').modal('hide');
popupMessage('Product added into queue.', 'alert-info');
<% end %>